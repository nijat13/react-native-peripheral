README
======

This Android library module is configured to work as a React Native library dependency.

The build configuration is simplified and should work seamlessly when included in other React Native projects.
